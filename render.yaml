services:
  - type: web
    name: tzu-chi-moz-lms
    runtime: static
    rootDir: TZU CHI MOZ LMS V2
    buildCommand: echo "Static site - no build required"
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
